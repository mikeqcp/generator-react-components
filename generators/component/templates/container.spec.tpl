import { expect } from 'chai';
import { spy } from 'sinon';

import { mapDispatchToProps } from '../<%= name %>.container';

describe('<%= className %>: Container', () => {
  describe('mapDispatchToProps', () => {
  });
});
